## Comments and Scores

1. Question 1: Creating a no-args constructor will allow Creating and employee without any initial data, which should be restricted. -2
2. Question 2: Should have used mapToInt() to convert Objects to primitives as objects are not added. Eventhough auto boxing and unboxing happening : -2
```java
mlistOfIntegers.stream().reduce((x, y) -> (x + y)).get();
```
3. Question 3: insert_pst.executeBatch() should be done under transaction: -2


